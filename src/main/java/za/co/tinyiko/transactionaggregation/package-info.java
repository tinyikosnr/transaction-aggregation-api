/**
 * Transaction Aggregation API — root package.
 *
 * <p>A modular-monolith Spring Boot application that ingests, validates, categorises,
 * persists and aggregates financial transactions from multiple upstream sources. Built on
 * Domain-Driven Design and Clean Architecture: each direct sub-package of this package is an
 * independent Spring Modulith application module with its own owned data and explicit
 * boundaries. See {@code documentation/} for the full architecture (SAD, TDS).
 */
package za.co.tinyiko.transactionaggregation;
